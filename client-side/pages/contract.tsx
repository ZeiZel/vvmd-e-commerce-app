import React from 'react';
import { ContractPage } from '../page-components';
import { withLayout } from '../layout/Layout';

const Contract = () => {
	return <ContractPage />;
};

export default withLayout(Contract);
